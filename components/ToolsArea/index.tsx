import { Download } from "@mui/icons-material";
import { IconButton } from "@mui/material";

export const ToolsArea = () => {
  return (
    <IconButton>
      <Download></Download>
    </IconButton>
  );
};
